<%= form_with(model: [travel_plan, chat], local: false) do |form| %>
  <% if chat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chat.errors.count, "件のエラー") %> のためチャットは保存出来ませんでした</h2>
      <ul>
      <% chat.errors.full_messages.each do |message| %>
        <%= message %>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, placeholder: "コメントを入力" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>