<div class="texts-wrapper">
  <div class="contents-title text-center">
    <%= image_tag 'camplet.png', :size =>'250x250' %>
    <h1 class = "mb-5"><%= @user.name %>のページ</h1>

    <p>名前: <%= @user.name %></p>

    <p>メールアドレス: <%= @user.email %></p>

    <% if current_user.id == @user.id %>
      <%= button_to "プロフィールを編集",  edit_user_registration_path(@user), method: :get, class: "btn btn-outline-info" %>
    <% end %>

    <br>
    <button type="button" class="btn btn-outline-info" style="background-color: #e3f2fd;">
      <%= link_to 'キャンプギア一覧', gears_path %>
    </button>
    <button type="button" class="btn btn-outline-info" style="background-color: #e3f2fd;">
      <%= link_to '所属チーム一覧', teams_path,id: 'rspec-edit' %>
    </button>
  </div>
</div>

