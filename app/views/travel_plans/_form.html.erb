<%= form_with(model: [@team, travel_plan]) do |form| %>

  <% if travel_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(travel_plan.errors.count, "error") %> prohibited this travel_plan from being saved:</h2>

      <ul>
        <% travel_plan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :start_schedule_at %>
    <%= form.datetime_select :start_schedule_at %>
  </div>

  <div class="field">
    <%= form.label :end_schedule_at %>
    <%= form.datetime_select :end_schedule_at %>
  </div>

  <div class="field">
    <%= form.label :持ち物 %>
    <%= form.collection_check_boxes(:label_ids, Label.all, :id, :name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
